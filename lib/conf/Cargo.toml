[package]
name = "conf"
version = "0.1.0"
edition = "2021"


[dependencies]
async-graphql = { version = "7.0", optional = true }
anyhow = "1.0"
serde = { version = "1.0", features = ["derive"] }


[features]
default = []
gql = ["async-graphql"]
