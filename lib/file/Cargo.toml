[package]
name = "file"
version = "0.1.0"
edition = "2021"


[dependencies]
serde = { version = "1.0", features = ["derive"] }
async-graphql = { version = "7.0", optional = true }
anyhow = "1.0"

[features]
default = []
gql = ["async-graphql"]
