[package]
name = "common"
version = "0.1.0"
edition = "2021"


[dependencies]
serde = { workspace = true, features = ["derive"] }
async-graphql = { workspace = true, optional = true }
nanoid = "0.4.x"


[features]
default = []
graphql = ["async-graphql"]
