[package]
name = "common"
version = "0.1.0"
edition = "2021"


[dependencies]
serde = { version = "1.0", features = ["derive"] }
async-graphql = { version = "7.0", optional = true }
nanoid = "0.4.x"


[features]
default = []
gql = ["async-graphql"]
